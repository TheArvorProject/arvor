# to compile it use ztc: https://github.com/ZeonTheFerret/ztc
# this code also can be compiled as an ELF and an .c,but it is not tha case here.
# Zeon (@zeontheferret — both github and discord.)
ztsl.start # start
}
func "nsm" 
sys.exec # compiles nsm
```shell
gcc -o ./usr/bin/nsm ./usr/bin/nsm.c
```
}
func "weld"
sys.exec # compiles weld
```shell

 g++ -std=c++23 -O2 \
    -Dallow_weld_repositories \
    -Dnflinux \
    -Darvor_version=\"8.1\" \
    usr/bin/weld.cpp -o ./usr/bin/weld \
    -lapt-pkg -lcurl

```
}
func "arvor_sys_info" # compiles arvor_sys_info
sys.exec
```
gcc -o usr/bin/arvor_sys_info  # notice that no build code is defined here,because it is made for our discord https://discord.gg/zQaGrHNgxB for specific bug reports. 
```
}
func "lxsm"
sys.exec # compiles lxsm
```shell
g++ -std=c++23 -O2 ./usr/bin/lxsm.cpp -o ./usr/bin/lxsm -ldl
```
}
func "debootstrap"
sys.exec # debootstraps arvor
```shell
sudo debootstrap \
  --arch=amd64 \
  --components=main,contrib,non-free,non-free-firmware \
  --include=xfsprogs,lvm2,python3-pyqt6,python3-pyqt6.qtsvg,mesa-utils,mesa-vulkan-drivers,libgl1-mesa-dri
  trixie \
  ./arvor \
  http://deb.debian.org/debian/ 
```
}
func "drivers"
sys.exec # install drivers
```
set -e

TARGET_DIR="./arvor"

cat << 'EOF' | sudo tee "$TARGET_DIR/etc/apt/sources.list"
deb http://deb.debian.org/debian/ trixie main contrib non-free non-free-firmware
deb-src http://deb.debian.org/debian/ trixie main contrib non-free non-free-firmware

deb http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware
deb-src http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware

deb http://deb.debian.org/debian/ trixie-updates main contrib non-free non-free-firmware
deb-src http://deb.debian.org/debian/ trixie-updates main contrib non-free non-free-firmware
EOF


sudo chroot "$TARGET_DIR" env DEBIAN_FRONTEND=noninteractive apt-get update
sudo chroot "$TARGET_DIR" env DEBIAN_FRONTEND=noninteractive apt-get install -y \
  firmware-amd-graphics \
  firmware-intel-sound \
  firmware-linux \
  firmware-linux-nonfree \
  firmware-mediatek \
  firmware-realtek
  
```
}
func "install_codes" # install the codes
sys.exec
```
sudo cp {./usr/bin/nsm./usr/bin/,./usr/bin/napt./usr/bin/,./usr/bin/lxsm,./usr/bin/arvor*,./usr/bin/arvor_sys_info,./usr/bin/nsmd_start} ./arvor/usr/bin
```
}
func "chroot"
text.show # prints the text
```
 █████  ██████  ██    ██  ██████  ██████      ██      ██ ███    ██ ██    ██ ██   ██
██   ██ ██   ██ ██    ██ ██    ██ ██   ██     ██      ██ ████   ██ ██    ██  ██ ██
███████ ██████  ██    ██ ██    ██ ██████      ██      ██ ██ ██  ██ ██    ██   ███
██   ██ ██   ██  ██  ██  ██    ██ ██   ██     ██      ██ ██  ██ ██ ██    ██  ██ ██
██   ██ ██   ██   ████    ██████  ██   ██     ███████ ██ ██   ████  ██████  ██   ██

Welcome To The Arvor Linux Chroot!
```
}
ztsl.end # end
